import React from 'react';
import './Loader.css'; // Assuming the CSS file is named Loader.css and located in the same directory

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
